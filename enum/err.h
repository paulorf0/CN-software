#pragma once
#ifndef ERR_H
#define ERR_H

#include <string>

typedef enum {
  SUCESS = 0,
  NOT_EXIST_VARS,
  WRONG_SYNTAX,
  ERROR_CREATING_VARIABLES,
  EXPR_COMPILATION_ERROR,
  MALLOC_ERROR,
  UNKNOW_ERROR,
  INVALID_INTERVAL,
  INVALID_FUNCTION,
  NEW_INTERVAL,
  GET_INPUT,
  ERROR,
  END_ERR
} ErrorCode;

#endif